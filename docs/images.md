# Images that are on the system

Show all images:
```
docker images
docker images -a
```

Stop all images
```
docker system prune
```

Erase images:
```
# forcing to remove running docker
docker rmi -f 7ccbbf771248
```